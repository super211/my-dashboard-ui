import { NumberIteratorPipe } from './number-iterator.pipe';

describe('NumberIteratorPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberIteratorPipe();
    expect(pipe).toBeTruthy();
  });
});
